<h2>商品詳細</h2>

<div>
  <div>
    <div>
      <%= image_tag @item.get_image(100,100) %> 
    </div>
    <div>
      <p>商品名 <%= @item.name %></p>
      <p>商品説明 <%= @item.introduction %></p>
      <p>
        税込価格<br>
        (税抜価格) 
        
        <%= @item.add_tax_price %>
        (<%= @item.price %>)円
      </p>
      <%= link_to "編集する", edit_admin_item_path(@item.id) %>
    </div>
  </div>
</div>