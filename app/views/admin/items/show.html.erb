<h2>商品詳細</h2>

<div>
  <div>
    <div>
      <%= image_tag @item.get_image(100,100) %> 
    </div>
    <div>
      <p>商品名 <%= @item.name %></p>
      <p>商品説明 <%= @item.introduction %></p>
      <p>
        税込価格<br>
        (税抜価格) 
        
        <%= @item.add_tax_price.to_s(:dalimited) %>
        (<%= @item.price %>)円
      </p>
    </div>
  </div>
</div>